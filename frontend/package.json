{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "node --experimental-strip-types tests/motion.test.mjs",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^7.0.0"
  }
}
